{
  "name": "@tkk-music/root",
  "type": "module",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "mobile:build": "npm run build -w @tkk-music/mobile",
    "mobile:dev": "npm run dev -w @tkk-music/mobile",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "typescript": "^7.0.2"
  }
}
